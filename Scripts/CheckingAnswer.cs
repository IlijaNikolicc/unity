using System;
using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;
using UnityEngine.UI;
using UnityEngine.UIElements;

public class CheckingAnswer : MonoBehaviour
{
    /*
    [SerializeField] QuizActivator quiz;
    public string correctAns = QuizActivator.correctAnswer;
    public List<UnityEngine.UI.Button> buttons = new List<UnityEngine.UI.Button>();

    void Start()
    {
        // Dodavanje funkcije za svako dugme u listi
        foreach (var button in buttons)
        {
            button.onClick.AddListener(() => ButoonPressed(button));
        }
    }
    
    public void ButoonPressed(UnityEngine.UI.Button button)
    {
        Debug.Log("Tacan odgovor " + correctAns);
        Debug.Log("Dugme odgovor " + button.GetComponentInChildren<TextMeshProUGUI>().text);
        if(button.GetComponentInChildren<TextMeshProUGUI>().text.Equals(correctAns))
        {
            Debug.Log("correct");
        }
        else
        {
            Debug.Log("wrong");
        }
       
    } 
    
    */
}
